on:
  schedule:
    - cron: '*/6 * * * *'
      timezone: Europe/Rome
      
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test schedule workflow
        run: echo "This is a test workflow that runs every 2 minutes."